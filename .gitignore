# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Secrets / local config
.env
.env.local

# The user's real catalog is private; ship only the schema + examples, never a populated catalog.
catalog/*.md
!catalog/README.md
!catalog/example-*.md

# Local dev / parallel-session state (never publish)
.claude/
.venv-review/

# Local launch-prep working doc (do not publish)
LAUNCH-CHECKLIST.md
